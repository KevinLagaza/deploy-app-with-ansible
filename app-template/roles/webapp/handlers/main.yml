---
- name: Restart webapp container
  docker_container:
    name: "{{ container_name }}"
    state: started
    restart: yes

- name: Restart nginx-proxy container
  community.docker.docker_container:
    name: nginx-proxy
    state: started
    restart: yes

...